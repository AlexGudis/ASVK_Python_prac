from fractions import Fraction as frac
# Удобный инструмент для представления чисел в виде рациональных дробей 
print(frac(1.1))

from decimal import Decimal as dec
# Грубо можно представить, что числа хранятся в виде десятичных цифр в памяти
d = dec(1.1)
print(d)